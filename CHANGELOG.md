# EasyCraft Launcher 0.4.0

## 핵심 변경 사항

- 전체 UI를 새로 디자인했습니다. 홈 / 콘텐츠 / 설정의 3개 화면으로 단순화했습니다.
- 홈에서 선택 인스턴스, 실행 버튼, 인스턴스 목록, 빠른 콘텐츠 검색을 한눈에 볼 수 있습니다.
- Microsoft 계정 로그아웃 버튼을 사이드바와 설정 화면에 추가했습니다.
- 프로그램 시작 시 업데이트를 먼저 확인합니다.
  - 최신 버전이면 첫 화면이 자동으로 닫히고 런처가 열립니다.
  - 새 버전이 있으면 `업데이트` / `나중에`를 선택합니다.
  - 업데이트를 선택한 경우에만 다운로드합니다.
- electron-updater의 빌드 생성 `app-update.yml`을 사용하는 표준 업데이트 흐름으로 변경했습니다.
- Minecraft 실행 경로를 인스턴스 전용 game 폴더 하나로 고정하고 `instance` 중복 경로 생성을 명시적으로 차단했습니다.
- 실행 때 매번 전체 SHA-1 검증을 반복하지 않고, 누락된 필수 파일만 core가 확인/다운로드하도록 실행 옵션을 정리했습니다.
- 병렬 다운로드 수를 늘려 첫 실행 준비 시간을 줄였습니다.
- 게임 실행 로그는 UI에 표시하지 않고 launcher-logs에만 저장합니다.
- 준비 중에도 `중지`를 눌러 이후 생성되는 Java 프로세스까지 종료하도록 유지했습니다.
- Modrinth 검색/설치/업데이트, 필수 의존 모드 확인, Iris 조건부 셰이더 탭, 인스턴스별 설정 기능을 새 UI에 통합했습니다.
- 한글 인스턴스 이름과 IME Enter 입력 처리를 유지합니다.
