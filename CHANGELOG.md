# EasyCraft Launcher 0.4.1 변경 내역

## UI
- 홈 오른쪽의 장식용 블록 3개를 제거했습니다.
- 왼쪽 위 `E` 글자 박스를 실제 EasyCraft 앱 아이콘으로 교체했습니다.
- 시작 업데이트 화면의 아이콘도 앱 아이콘으로 통일했습니다.
- 로그인된 계정에는 Minecraft 스킨의 얼굴 영역을 표시합니다. 스킨 정보를 가져오지 못하면 닉네임 첫 글자를 대신 표시합니다.

## Minecraft 실행 / 중지
- 실행 직전에 Modrinth 복구 네트워크 작업을 기다리던 과정을 제거해 실행 버튼 반응을 빠르게 했습니다.
- 한 번 정상 준비된 인스턴스는 같은 앱 실행 세션에서 `minecraft-java-core.start()` 기반 빠른 재실행 캐시를 사용합니다.
- `Launch()/start()` Promise 성공도 게임 시작 신호로 사용하여 특정 로그 문자열이 나오지 않아 실행 상태를 못 잡던 경우를 보강했습니다.
- Microsoft 인증은 매번 갱신하지 않고 오래된 경우에만 갱신합니다.
- 각 게임에 EasyCraft 고유 JVM marker를 넣어 실행 프로세스를 더 정확하게 찾습니다.
- Java PID를 찾은 뒤 캐시하고, 종료 시 `taskkill /PID /T /F`를 우선 사용해 종료 시간을 단축했습니다.
- 준비 중 중지는 UI에서 즉시 완료 처리하며, 뒤늦게 생성되는 Java 프로세스도 자동 종료합니다.
- 다운로드 동시 작업 수와 네트워크 timeout을 안정성 위주로 조정했습니다.

## 모드 / 콘텐츠
- Modrinth에서 설치한 모드, 리소스팩, 셰이더의 아이콘을 설치 목록에도 표시합니다.
- 0.4.0 이전에 설치한 Modrinth 콘텐츠도 프로젝트 ID가 있으면 아이콘을 자동 보완해 저장합니다.

## 계정
- Microsoft 로그인 후 Minecraft 프로필의 스킨 정보를 조회하여 왼쪽 아래 계정 카드에 캐릭터 얼굴을 표시합니다.
- 기존 로그아웃 기능은 유지됩니다.
